# Résumé files

Add the final files here:

- `robert-mcdermott-resume.pdf`
- `robert-mcdermott-resume.docx` (optional)

After adding them, update `app/resume/page.tsx` so the disabled controls become download links. Keep the file free of private address, phone, credentials, or other details that should not be public.
